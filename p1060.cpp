#include<bits/stdc++.h>
using namespace std;
const int qwe = 10005;
int n, m;
int a[qwe];
int main()
{
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
    scanf("%d", &n);
    scanf("%d", &m);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i]);
    }
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            if (1)
            {
                f[j] = max(f[j], f[j-])
            }
            
        }
        
    }
    
    return 0;
}