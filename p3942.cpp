#include<bits/stdc++.h>
using namespace std;
int n, k, t;
int main()
{
    scanf("%d%d%d", &n, &k, &t);
    if(t == 2 && k >= 2)
    {
        printf("1");
    }
    for(int i = 1; i <= n; i++)
    {
        scanf("%d", &)
    }
}