#include <bits/stdc++.h>
using namespace std;
const int qwe = 10005;
int a[qwe], m, n;
int main()
{
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
    scanf("%d%d", &m, &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &a[i + 1]);
    }
    for (int i = 0; i < n; i++)
    {
        
    }
    
    return 0;
}