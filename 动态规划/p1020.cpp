#include<bits/stdc++.h>
using namespace std;
int n;
int a[qwe], b[qwe];
int main(){
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
    scanf("%d", &n);
    for(int i = 1; i <= n; i++)
    {
        scanf("%d %d", &a[i], &b[i]);
    }
    return 0;
}