#include <bits/stdc++.h>
using namespace std;
const int qwe = 10005;
int n, c;
int a[qwe];
int f[qwe][qwe];
int main()
{
    freopen("test.in", "r", stdin);
    freopen("test.out", "w", stdout);
    scanf("%d %d", &n, &c);
    for (int i = 1; i <= n; i++)
    {
        scanf("%d", &a[i])
    }
    for (int l = c; l <= n && l >= 1; l++)
    {
        for (int r = l + 1; r <= n; r++)
        {
            
        }
    }
    return 0;
}